name: Hola Mundo

on: [push, pull_request]


jobs:
  hola:
    runs-on: ubuntu-latest
    
    steps:
    - name: Hola Mundo
      run: echo "Hola mundo"

    - name: touch file
      run: touch hola.txt

    - name: Set text
      run: echo "Hola mundo" >> hola.txt
    
    - name: Read file
      run: cat hola.txt

  other:
    needs: [hola]
    runs-on: ubuntu-latest
    
    steps:
    - name: LS
      run: ls -al
      